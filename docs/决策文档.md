## 2026-08-16｜决策协作方式
- 决策：后续每轮由决策 Agent 输出可直接交给执行 Agent 的完整执行提示词；执行结果返回后再复核并决定下一步。
- 约束：提示词明确范围、硬约束、验收和停止条件；回复末尾单独保留简洁决策日志，不与执行 Agent 工作日志混用。

## 2026-08-16｜阶段 2 实施决策
- 决策：G1 前端设计门禁通过；阶段 2 按完整纵向切片实施，不再拆成独立正式子阶段。
- 决策：bootstrap.py 负责幂等创建初始管理员并初始化/校验三档 Dataset；seed_demo.py 仅用于可选演示数据。
- 决策：鉴权每请求回查用户与会话，角色以数据库当前值为准；停用、重置密码、角色变更下一请求立即生效。
- 决策：允许管理员自我降权，但任何操作不得使 active admin 数量降为 0；禁止管理员停用自己。
- 决策：第一版不建设密码复杂度体系，只做基本安全校验与 bcrypt 哈希。
- 约束：Dataset bootstrap 实现前必须核对原 RAG 真实 Dataset/member API；若 ID/name/member 语义与冻结设计冲突则硬停并上报。


## 2026-08-16｜阶段 2 首次复核
- 结论：阶段 2 主体实现通过，暂不最终验收；先进行一轮小范围收尾修复。
- 必修：RAG Client 连接池生命周期与 GET 重试、member 角色/整体状态真实性、用户 disabled_at 状态转换、JWT iat 同秒处理、最后 active admin 并发保护、前端单次登录与登录态恢复。
- 约束：不新增表/迁移/权限系统，不扩展密码策略，不进入阶段 3；修复后重新执行阶段 2 全量验收。


## 2026-08-16｜原 RAG 契约独立复核
- 结论：已通过 `WangBeitang/ai_knowledge_base_after_class` 原 RAG 仓库独立核实阶段 2 集成契约，执行 Agent 对 Dataset ID、X-User-Id、member、owner 及幂等语义的描述准确。
- 决策：保持当前三档 Dataset + 固定服务身份映射不变；成员校验必须同时核对 user_id 与 role。
- 约束：无权访问的既有同 ID Dataset 不自动接管；发生 ID 冲突时 bootstrap 明确失败并人工处理。


## 2026-08-16｜阶段 2 二次复核
- 结论：83c27c3 后端及上一轮主要修复项复核通过。
- 待收尾：当前管理员自我降权后前端需立即同步角色并退出管理区；管理员重置自己密码后需立即清理本地登录态并返回登录页。
- 约束：仅做前端小修，不再调整后端，不进入阶段 3；完成后进行阶段 2 最终验收。

## 2026-08-16｜阶段 2 最终验收
- 结论：Stage 2「身份与 Dataset 初始化」最终验收通过并冻结。
- 验收基线：40f1bc8 完成主体实现，83c27c3 完成第一轮复核修复，0d2bc91 完成最终前端身份同步收尾。
- 决策：允许进入 Stage 3「知识管理纵向切片」；阶段 2 后续仅接受明确缺陷修复，不再扩展功能。

## 2026-08-16｜阶段 3 决策对齐
- 已锁定：三档 scope 同时映射 Dataset 与 Document visibility；Stage 3 仅支持 PDF；多文件平台循环单发；任务由查询轮询推进；rebuild/delete/replace/Chunk 均按上游真实状态实现，不增加 Worker。
- 已锁定：替换强制同 scope，新文档失败绝不删除旧文档；Chunk 正文只读，平台字段由 Adapter 统一映射。
- 待确认：原 RAG Chunk 接口当前最多返回前 100 条且无分页参数；推荐做最小管理 API 分页兼容补丁，而不是让平台永久只管理前 100 个 Chunk。

## 2026-08-16｜阶段 3 实施决策
- 决策：Stage 3 完整实现导入、任务轮询、文档查询/重建/替换/删除及 Chunk 查看/启停；仅支持 PDF，多文件由平台循环调用原 RAG 单文件上传。
- 决策：三档范围同时映射 Dataset 与 Document visibility：external_public→public、internal_shared→shared、admin_private→private；原 RAG 管理操作固定使用管理员服务身份。
- 决策：替换强制同 scope；新文档失败绝不删除旧文档；新文档成功后通过数据库行锁保证旧文档只删除一次。
- 决策：Chunk 平台字段由 Adapter 映射，正文只读且不落 MySQL；索引版本冲突返回 INDEX_VERSION_CONFLICT。
- 决策：允许对原 RAG 做最小 Chunk 管理分页兼容补丁；利用当前版本内连续稳定的 chunk_index 按 [offset, offset+limit) 范围查询，不修改导入、检索或 Milvus Schema。

## 2026-08-16｜执行提示词长度策略
- 决策：当前 Stage 3 提示词保持不变；后续阶段改为“总决策 + 分批执行”，每批完成并复核后再下发下一批。
- 原则：执行提示词只保留目标、冻结决策、实现要求、硬停条件、验收和汇报；决策分析不大量重复给执行 Agent。
- 目标：降低长上下文注意力流失，同时保留关键约束和阶段级一致性。

## 2026-08-17｜阶段 3 首次代码复核
- 结论：Stage 3 主体实现成立，暂不验收；需要一轮小范围修复。
- 阻断：原 RAG Chunk offset 分页补丁尚未在远端仓库确认。
- 必修：replace 失败终态不可被后续轮询复活、terminal 停止轮询、completed 节点使用最新上游状态、Chunk 详情 Drawer 与版本冲突刷新。
- 约束：不扩大 Stage 3，不进入 Stage 4。

## 2026-08-17｜阶段 3 RAG 分页补丁复核
- 结论：原 RAG 分页 commit c88ba07e 已推送远端，但当前实现仍受 2000 条查询上限限制，不符合 Stage 3 真分页决策。
- 修正：分页必须直接按当前 index_version 的 chunk_index 范围过滤，不再“读取前 N 条后 Python 切片”。
- 其余 Stage 3 首轮复核问题保持不变，统一交执行 Agent 一轮收尾。

## 2026-08-17｜阶段 3 最终验收
- 结论：Stage 3「知识管理纵向切片」最终验收通过并冻结。
- 验收基线：原 RAG f09d562 完成 Chunk 真分页；knowledge-platform 96981939 完成 Stage 3 首轮复核收尾。
- 已确认：导入、任务状态、文档管理、rebuild/delete、replace 失败安全、Chunk 真分页/只读查看/启停及版本冲突均满足阶段约束。
- 决策：允许进入 Stage 4；Stage 3 后续仅处理明确缺陷，不再扩展功能。

## 2026-08-17｜Stage 4 决策冻结
决策：Stage 4 完成内部会话、FAQ 精确短路、RAG SSE/Citation、历史记录、访问日志和 Trace Token 回填完整纵向切片；同一会话禁止并发 Turn。
决策：平台只输出冻结 SSE 契约；FAQ 命中不调用 RAG、不伪造引用；RAG Citation 缺失字段保持真实 null。
决策：允许原 RAG 做最小 Trace Token Usage 可观测契约补强，明确区分“真实 0”与“未知”；未知 Token 永远保存 null。
约束：流式增量只驻留内存，终态后一次性持久化；不进入 Stage 5，不引入新的异步基础设施，不重构 RAG 核心。


## 2026-08-17｜Stage 4 首轮代码复核
- 结论：Stage 4 主体实现成立，暂不冻结；需一轮针对性修复。
- 阻断：浏览器断开后不能释放仍在运行的上游 Session；Trace Token 必须代表可证明完整的本轮 LLM 用量，否则统一为 NULL。
- 必修：真实网络断流也执行 status 终态兜底；RAG Query Adapter 收紧 SSE/status/final 契约校验。
- 约束：不扩大 Stage 4，不新增后台基础设施，不进入 Stage 5。

## 2026-08-17｜Stage 4 二次代码复核
- 结论：首轮修复方向成立，但 Stage 4 仍不冻结；需再做一轮聚焦收尾。
- 阻断：平台仍将“下游已 error”误作“上游已 terminal”，status=processing 时会释放同 Session 占位；pre-submit/submitting 生命周期及并发 orphan recovery 仍不完整。
- 阻断：整轮 Token 对 partial usage 仍可能误判 available=true，且模型 Planner Token 尚未计入。
- 必修：拆分 release-safe 语义、补 submitting/恢复并发保护、模型 Planner 保守 unavailable、收紧 status 类型校验。
- 约束：不新增异步基础设施，不扩大 RAG 核心，不进入 Stage 5。


## 2026-08-17｜Stage 4 三次代码复核
- 结论：二次修复主体方向正确，但 Stage 4 仍暂不冻结，需最后一轮契约/取消安全收尾。
- 阻断：平台依赖的 status=404 absent 语义与真实 RAG 不一致，且 accepted POST 尚无同步 pending 注册；POST 契约异常仍可能被错误视为未接受。
- 阻断：recover 在 CancelledError 下可能残留 recovering，占位持久化标志存在 commit 后取消窗口；status 尚未绑定校验 session_id。
- 待收尾：Token 非负整数严格性与 orphan completed Token backfill。
- 约束：只做最小 Stage 4 修复，不扩业务、不新增异步基础设施、不进入 Stage 5。

## 2026-08-17｜Stage 4 V1 验收口径调整
- 决策：Stage 4 第一版以冻结文档明确需求、正常主链路和常规异常链路跑通为验收标准，不继续追逐极端网络/取消时序边界。
- 结论：fbaebd8 + f48b2c3 进入 Stage 4 最终验收，不执行第三轮边界修复。
- 延后：acceptance ambiguity、pending 注册竞态、CancelledError recovery、status session_id 强绑定、畸形 Token metadata、orphan Token 回填列入 V2 技术债。
- 约束：同 Session 常规并发禁止、未知 Token 保存 NULL 等原冻结要求保持不变。

## 2026-08-17｜Stage 4 最终验收（冻结）
- 结论：Stage 4「内部问答纵向切片」最终验收通过并冻结。
- 验收基线：knowledge-platform fbaebd8（含 f6c32b5 首轮复核 + fbaebd8 二次复核）；ai_knowledge_base_after_class f48b2c3（含 98f3926 首轮 + f48b2c3 二次复核）。
- 验收证据：平台 pytest 233 passed；原 RAG pytest 607 passed；真实 smoke 31/31 通过（SSE 契约、FAQ 精确短路、Citation、Trace Token、会话并发保护、断流恢复）。
- 决策：允许进入 Stage 5；Stage 4 后续仅接受明确缺陷修复，不再扩展功能。
- 以下 6 项作为 V2 技术债留存，禁止在 Stage 5 顺手修复，禁止重新打开 Stage 4 边界审查：
  1. accepted POST / pending 注册极端竞态；
  2. HTTP acceptance ambiguity 完整状态机；
  3. recover CancelledError cancellation-safe；
  4. status session_id 强绑定；
  5. provider 畸形 Token 数值严格校验；
  6. orphan recovered Turn Token 补回填。

## 2026-08-17｜Stage 4 最终验收
- 结论：Stage 4「内部问答纵向切片」按 V1 口径最终验收通过并冻结。
- 验收基线：knowledge-platform fbaebd8；原 RAG f48b2c3。
- 已确认：会话、FAQ 精确短路、RAG SSE/Citation、历史记录、访问日志、Trace Token 回填及常规同 Session 并发保护满足本期要求。
- 延后：6 项极端时序/异常恢复问题作为 V2 技术债，不阻断第一版。
- 决策：允许进入 Stage 5；Stage 5 按“FAQ 闭环 → 知识缺口 → 看板与审计”三批实施，先执行 Batch 1 FAQ 闭环。

## 2026-08-17｜Stage 5 Batch 1 首轮代码复核
- 结论：FAQ 闭环主体实现成立，暂不冻结；需一轮小范围收尾修复。
- 阻断：unpublished FAQ 经 PATCH 会重新写入 Redis；sync:retry 响应缺统一 data 壳导致前端重试必失败；同 scope 连续变更可产生并行同步分叉并残留旧版 RAG FAQ 文档。
- 必修：PATCH 空答案校验、同步成功清理 rag_sync_error、FAQ MySQL 事务完成后再执行 Redis/RAG 外部副作用。
- 约束：不新增 Worker/队列/新表，不修改原 RAG，不进入 Stage 5 Batch 2/3。

## 2026-08-17｜Stage 5 Batch 1 二次代码复核
- 结论：首轮 5 项功能问题已修正，Batch 1 仅剩事务顺序一处阻断。
- 阻断：FAQ MySQL 当前仍在 Redis/RAG 外部副作用之后才真正 commit，与冻结的“业务事务成功后再同步”顺序不符。
- 必修：建立明确 commit 边界，并补独立 Session 验证；随后做最小真实 RAG FAQ 冒烟。
- 约束：不扩大并发强一致性设计，不新增基础设施，不进入 Batch 2/3。

## 2026-08-17｜Stage 5 Batch 1 最终验收
- 结论：FAQ 闭环最终验收通过并冻结，允许进入 Stage 5 Batch 2。
- 验收基线：knowledge-platform 08ff82e；原 RAG f48b2c3。
- 已确认：FAQ 候选/审核、MySQL+Redis、三档 Markdown 同步、旧版清理、失败重试、单 scope 顺序同步、事务 commit 边界及真实 RAG multipart 上传链路成立。
- 验证：平台全量 258 passed；FAQ targeted 25 passed；真实 RAG FAQ smoke 14/14。
- 决策：下一批仅实现知识缺口闭环，不进入看板与完整审计查询。

## 2026-08-17｜Stage 5 Batch 2 首轮代码复核
- 结论：知识缺口闭环主体实现成立，暂不冻结，仅需一处数据时间语义收尾。
- 必修：knowledge_gap_candidates.created_at/last_seen_at 必须取真实访问日志首次/最近发生时间，不能使用管理员 analyze 时间。
- 已确认：缺口判定、聚合去重、处理后不复活、ignore/resolve 状态机、权限与审计符合冻结要求。
- 约束：只修时间字段，不扩大 Batch 2，不进入看板与审计查询。

## 2026-08-17｜Stage 5 Batch 2 最终验收
- 结论：知识缺口闭环最终验收通过并冻结，允许进入 Stage 5 Batch 3。
- 验收基线：knowledge-platform 8e222bc；原 RAG f48b2c3。
- 已确认：缺口判定、日志聚合去重、ignore/resolve、处理后不复活、审计及首次/最近发生时间语义符合冻结要求。
- 验证：test_knowledge_gap 16 passed；平台全量 274 passed；ruff 全绿。
- 决策：Batch 3 仅实现运营看板与审计查询，完成后进行 Stage 5 总验收。

## 2026-08-17｜Stage 5 Batch 3 / Stage 5 最终验收
- 结论：Stage 5 Batch 3「运营看板与审计查询」验收通过，Stage 5 整体最终验收通过并冻结。
- 验收基线：knowledge-platform b3f5054；原 RAG f48b2c3。
- 已确认：Dashboard summary/trends/top-questions/top-documents 基于真实 qa_access_logs，Token 缺失保持 null 并提供 coverage_rate；审计查询支持冻结分页/筛选/排序、admin 权限及敏感字段响应兜底；Batch 3 未引入 Worker/ES/ClickHouse 等新基础设施。
- 验证证据：新增 Dashboard 10 + Audit 5 项集成测试；执行 Agent 全量 pytest 289 passed，ruff、前端 type-check/lint/build 全过。
- 延后：Dashboard 大数据量 SQL 聚合/Top Documents 批量关联、Audit 嵌套 JSON 递归脱敏作为后续性能与安全硬化项，不阻断 V1。
- 决策：允许进入 Stage 6；Stage 6 分两批执行，Batch 1 仅完成外部知识 API 与契约测试，Batch 2 再进行目标云服务器部署与最终验收。

## 2026-08-17｜Stage 6 Batch 1 验收
- 结论：Stage 6 Batch 1「外部知识 API 与契约测试」功能验收通过，允许进入 Batch 2 目标云服务器部署验收。
- 已确认：外部请求固定 external_public 范围/外部 Dataset/外部服务身份；FAQ 精确命中与 RAG 路径复用 Stage 4 编排；external_user_id 仅保存 HMAC 哈希；Service API Key 不落库；契约测试 19 passed、平台全量 308 passed、ruff 全绿。
- 边界：外部首问极端并发双 Session、SECRET_KEY 轮换导致外部用户 hash 重算作为后续优化，不阻断 V1。
- 说明：提交 b3e27fa 尚未推送远端，因此本轮未声明完成独立远端代码复核；Stage 6 最终冻结前必须保证最终部署 commit 可在远端复现。
- 决策：Batch 2 仅做部署文件收口、目标云服务器真实部署、公网/外部 API 冒烟及最终验收，不再扩展业务功能。

## 2026-08-17｜Stage 6 Batch 2 部署代码复核
- 结论：Stage 6 本地业务实现与部署方案主体成立，但 Batch 2 暂不最终冻结；目标机部署前需完成一轮最小部署修复。
- 阻断：MySQL 初始化脚本依赖 MYSQL_APP_USER/MYSQL_APP_PASSWORD，但 mysql 容器未注入对应环境变量，首次初始化会失败。
- 必修：frontend Nginx 继续向 backend 转发真实 X-Real-IP/X-Forwarded-For/X-Forwarded-Proto；部署 smoke 的正常内部/外部 RAG 问答必须要求 final，不能将 error 视为部署成功。
- 已确认：Stage 6 Batch 1 外部 API 契约、固定 external_public 权限、Service Key、外部用户 HMAC、FAQ/RAG 共用编排成立；f98e9f0 已可从 GitHub 远端读取，远端可复现门禁解除。
- 决策：完成上述最小修复后不再继续本地功能开发，直接进入目标云服务器真实部署与 V1 最终验收。

## 2026-08-18｜Stage 6 Pre-Deploy 最终代码复核
- 结论：上云前 3 项最小修复验收通过，Stage 6 本地开发与部署代码收口完成并冻结。
- 验收基线：knowledge-platform 005f778；原 RAG f48b2c3。
- 已确认：MySQL 应用账号变量已正确注入容器；双层 Nginx 客户端转发头已正确透传；部署 smoke 的正常内部/外部 RAG 链路均必须取得唯一 final，error 不再视为部署成功。
- 范围确认：005f778 相对 f98e9f0 仅修改 docker-compose、smoke、frontend nginx 与决策文档，无业务功能扩张。
- 远端状态：GitHub main 已指向 005f778，远端可复现门禁解除。
- 决策：停止本地功能开发，下一步直接进入目标云服务器真实部署；只有取得目标机 Compose/init/smoke/公网 TLS/浏览器验收等真实证据后，才进行 Stage 6 / V1 最终冻结。

## 2026-08-18｜Stage 6 部署拓扑初步决策
- 结论：原 RAG 的计算服务优先部署至独立 GPU 服务器；知识中台本身不需要 GPU，不与模型计算服务强行混部。
- V1 原则：现有 Mongo/Milvus/MinIO/etcd 暂不迁移，优先保持现有数据基础设施稳定，由 GPU RAG 服务通过内网访问。
- 待确认：当前数据服务器疑似仅约 4GB 内存且已有多个重型容器，是否继续承载 knowledge-platform 需先检查实际内存、CPU 与各容器资源占用。
- 决策：资源检查完成后，再在“当前服务器承载中台”与“新增普通 CPU 服务器承载中台”之间二选一。